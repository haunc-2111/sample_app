<% provide :title, t("user.index.title") %>
<h1><%= t "user.index.title" %></h1>
<%= paginate @users, theme: "twitter-bootstrap-3" %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>
<%= paginate @users, theme: "twitter-bootstrap-3" %>
